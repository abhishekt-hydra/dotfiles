# Host-specific config

Create a folder named after `hostname -s`, then add `.zsh` snippets:

```txt
hosts/my-laptop/work.zsh
hosts/gaming-pc/gpu.zsh
```

These are loaded by `.zshrc` after common + OS topics.
